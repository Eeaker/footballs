base_config: gsr_detection_tracking_conf012_tvcalib_pitch_audit_v1.yaml

# Frozen smallest effective weight from the candidate-ranking audit.
# Experimental ablation only; not a promoted default.
linking:
  pitch_reranking:
    enabled: true
    mode: apply
    weight: 0.10
    baseline_distance_scale: 160.0
    pitch_distance_scale: 10.0
    require_all_candidates_usable: true
