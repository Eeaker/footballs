base_config: archive/rejected/number_region_cost_int_ata.yaml

# Solve Hungarian identity assignment independently inside each detected
# broadcast scene segment. This resets one-to-one roster competition at scene
# cuts, so one action does not steal identities from another action.
identity:
  assignment_scope: scene_segment
